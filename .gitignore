# secrets
.env
.env.local

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# 데이터 캐시 (백필/일별 수집 원본 — 용량 큼, 재생성 가능)
batch/data/cache/

# 배치 산출물 (~63MB) — main에는 커밋하지 않는다.
# GitHub Actions가 매일 data 브랜치에 force-push (daily-batch.yml 참고)
web/public/data/

# node
node_modules/
web/.next/
web/out/
.vercel/

# os
.DS_Store

# 마케팅 워크스페이스 — 별도 private 저장소로 관리한다 (블로그 소스 + 마케팅 전략 문서).
# 이 저장소는 public이므로 여기에 커밋하지 않는다.
kscreener-workspace/
