# 공개 준비 실행 가이드

주식 시그널 스크리너를 정식 공개(도메인 + 광고 수익화)하기까지 **해야 할 일을 순서대로** 정리했습니다.
각 단계는 **내가(개발) 할 일**과 **직접 하셔야 할 일**을 구분했습니다. 체크박스로 진행 상황을 추적하세요.

> 결론부터: **도메인을 먼저 사서 붙인 뒤 → 카카오 애드핏(쉬움) → 구글 애드센스(심사 김)** 순서가 가장 매끄럽습니다.
> 애드센스는 `*.vercel.app` 같은 공유 서브도메인에서는 승인이 거의 안 나므로 **도메인이 사실상 선행 조건**입니다.

---

## 0. 사전 완료 상태 (이미 되어 있음)

- [x] 면책조항 페이지 `/disclaimer`
- [x] 개인정보처리방침 페이지 `/privacy` (애드센스 필수 요건)
- [x] 콘텐츠/가이드 페이지 `/guide`, `/guide/chart-patterns`, `/guide/candlestick`
- [x] 광고 슬롯 자리(placeholder) 전 페이지 배치
- [x] 하루 지연 시세 고지 + 투자 유의 문구

→ 애드센스 심사에서 자주 걸리는 **개인정보처리방침·면책·콘텐츠**는 이미 갖춰졌습니다.

---

## 1단계 · 도메인 구매 ⭐ (가장 먼저)

### 왜 먼저?
- 애드센스는 자체 도메인을 강하게 선호(공유 서브도메인 승인률 매우 낮음).
- 브랜드·신뢰도·SEO에 유리. 한 번 정하면 바꾸기 번거로우니 초반에 확정.

### 어디서 살까 — 저렴한 곳 추천 (갱신가까지 고려)

| 등록업체 | .com 대략 가격(연) | 특징 | 추천도 |
|---|---|---|---|
| **Cloudflare Registrar** | **약 $10 (≈1.4만원)** | **원가 판매·마진 0, 갱신도 같은 가격**, WHOIS 보호 무료, 숨은 비용 없음 | ★★★ 최우선 |
| Porkbun | 약 $11 | 갱신도 비교적 저렴, WHOIS 무료, UI 깔끔 | ★★☆ |
| Namecheap | 첫해 약 $6~11 / 갱신 ~$14 | 첫해 프로모션 저렴(갱신은 오름) | ★★☆ |
| 가비아 / 후이즈(국내) | .kr 약 1~2만원 (프로모션 시 500원~) | `.kr`·`.co.kr` 취급, 한국어 지원·국내 결제 | ★★☆ (.kr 원할 때) |

> **핵심 팁**: 많은 업체가 "첫해 990원" 식으로 싸게 팔고 **갱신 때 몇 배로 올립니다**. 오래 운영할 거라면
> **첫해가 아니라 "갱신 가격"이 싼 곳**이 진짜 저렴합니다. 그 기준에서 **Cloudflare Registrar가 가장 유리**합니다(항상 원가).

### 도메인 이름·확장자 선택
- `.com` : 글로벌·신뢰·SEO에 가장 무난 (1순위 추천)
- `.kr` / `.co.kr` : "한국 서비스" 느낌, 국내 업체에서 저렴하게. 단 Cloudflare는 `.kr` 미취급 → 가비아/후이즈 이용
- 이름 예시 방향: 서비스 성격(시그널/스크리너/차트/주식) + 짧고 기억하기 쉬운 것

### 할 일
- [ ] 도메인 이름 후보 3~5개 정하기
- [ ] 등록업체에서 검색 → 구매 (Cloudflare Registrar 권장: dash.cloudflare.com → Domain Registration → Register)
- [ ] WHOIS 개인정보 보호(도메인 프라이버시) 켜기 — 대부분 무료
- [ ] **자동 갱신(auto-renew) 켜기** (만료로 도메인 뺏기는 사고 방지)

> 결제 수단(카드)은 **직접** 입력하셔야 합니다. 계정 생성·결제는 제가 대신 못 합니다.

---

## 2단계 · Vercel에 도메인 연결

구매한 도메인을 지금 배포 중인 Vercel 프로젝트에 연결합니다. (Production Branch = `data`)

### 할 일
- [ ] Vercel → 프로젝트 → **Settings → Domains** → 도메인 입력해 **Add**
- [ ] Vercel이 알려주는 DNS 레코드를 **등록업체(또는 Cloudflare) DNS**에 입력
  - apex(예: `example.com`): **A 레코드 `76.76.21.21`**
  - `www`: **CNAME `cname.vercel-dns.com`**
  - (Cloudflare DNS 사용 시: 처음엔 **회색 구름=DNS only**로 두면 Vercel SSL이 깔끔하게 발급됨)
- [ ] `www` → apex(또는 그 반대) 중 **하나로 리다이렉트 통일** (Vercel Domains에서 설정)
- [ ] HTTPS 자물쇠(SSL) 자동 발급 확인 (몇 분~수십 분)
- [ ] 새 도메인으로 접속해 정상 표시 확인

> 이 단계는 **화면 클릭 위주**라 제가 옆에서 값(레코드)을 정확히 짚어드릴 수 있습니다. 진행 시 Vercel 화면 스크린샷 주세요.

---

## 3단계 · SEO 기본기 (robots / sitemap / 서치콘솔)

애드센스 심사와 검색 노출 모두에 도움이 됩니다. **이건 제가 코드로 만들어 드릴 수 있습니다.**

### 할 일
- [x] `robots.txt` + `sitemap.xml` 생성 → **(개발) 완료**
- [x] 네이버 서치어드바이저 등록 → **완료**
- [ ] **Google Search Console 등록 + Google Analytics 설치** → 단계별 순서는 **[ANALYTICS_GUIDE.md](ANALYTICS_GUIDE.md)** 참고 (코드는 이미 심어둠, 값만 넣으면 됨) → **직접**

---

## 4단계 · 카카오 애드핏 신청·연동 (한국 광고, 쉬움·빠름 → 먼저)

애드센스보다 **심사가 빠르고 요건이 관대**해서, 도메인 붙자마자 이걸로 먼저 수익화를 시작하는 걸 권합니다.

### 특징
- **개인도 신청 가능**(사업자등록 없어도 됨), 국내 서비스
- 심사 보통 **1~2일**, 국내 계좌로 정산
- adfit.kakao.com

### 할 일
- [ ] 카카오 계정으로 **카카오 애드핏(adfit.kakao.com)** 가입 → 매체 관리자
- [ ] **매체(웹사이트) 등록** → 도메인 입력 → 심사 신청
- [ ] 심사 통과 후 **광고단위 만들기**(예: 반응형/배너/모바일) → **스크립트 코드** 발급
- [ ] 발급된 코드를 알려주시면 → **(개발) 내가 `AdSlot`을 실제 애드핏 코드로 교체**
- [ ] 정산 정보(본인 인증·계좌) 등록 → **직접**

---

## 5단계 · 구글 애드센스 신청·연동 (심사 김 → 콘텐츠·트래픽 쌓인 뒤)

단가가 높지만 심사가 까다롭습니다. 도메인·콘텐츠·개인정보처리방침이 갖춰진 뒤 신청하세요.

### 특징
- **자체 도메인 사실상 필수**(공유 서브도메인 반려 잦음)
- 심사 **며칠~수주**, 자주 나오는 반려 사유: "가치 없는 콘텐츠(low value content)" → 글·트래픽 보강 필요
- 지급 최소 **$100**, **PIN 우편 인증** + 계좌 등록, 세금정보(개인) 입력

### 할 일
- [ ] **애드센스(adsense.google.com)** 가입 → 사이트 추가
- [ ] 애드센스가 주는 **연결 코드(head 스니펫)** + **`ads.txt`** 요구 → **(개발) 내가 삽입** (게시자 ID `pub-XXXX` 알려주세요)
- [ ] 심사 신청 → 승인 대기
- [ ] 승인 후 광고 단위 생성 → **(개발) 내가 `AdSlot`을 실제 애드센스 코드로 교체**
- [ ] 지급 정보(주소·PIN·계좌·세금정보) 등록 → **직접**

> ⚠️ **애드센스 승인 팁**: 심사 때는 **광고를 과하게 넣지 마세요**. 슬롯은 넉넉히 만들어 뒀지만, 승인 심사 중엔
> 페이지당 광고 수를 줄이고(또는 애드핏만 노출), **승인 후에 점진적으로 늘리는 것**이 안전합니다.
> 콘텐츠 대비 광고가 많으면 정책 위반·반려가 됩니다.

---

## 6단계 · 광고 코드 실제 삽입 (내가 담당)

현재는 "광고" 글자만 있는 **자리(placeholder)** 상태입니다. 계정 승인 후:

- [ ] 애드핏/애드센스 **게시자 ID·슬롯 코드**를 나에게 전달
- [ ] **(개발) `components/AdSlot.tsx`를 실제 광고 태그로 교체** (슬롯 id별로 매핑)
- [ ] 필요 시 `ads.txt`, head 스니펫, 지연 로딩·레이아웃 시프트 최소화 처리
- [ ] 실제 광고 노출·클릭 정상 동작 확인 후 배포

---

## 7단계 · 운영·모니터링 (공개 후)

- [ ] 애드센스/애드핏 수익 리포트 주기적 확인
- [ ] Search Console 색인·검색 유입 확인, 콘텐츠 추가로 트래픽 성장
- [ ] 광고 밀도 조정(수익 vs 사용자 경험 균형)
- [ ] 배치(데이터 갱신)·사이트 정상 동작 모니터링

---

## 비용 요약 (대략, 환율·프로모션에 따라 변동)

| 항목 | 비용 | 비고 |
|---|---|---|
| 도메인(.com, Cloudflare) | **연 ~1.4만원** | 갱신도 동일가. 가장 저렴·투명 |
| 도메인(.kr, 국내) | 연 ~1~2만원 | 프로모션 시 훨씬 쌈 |
| Vercel 호스팅 | **0원** | Hobby 무료 (현재 그대로) |
| GitHub Actions 배치 | **0원** | public 레포 무제한 무료 |
| 애드핏/애드센스 | **0원** | 수익 발생 → 정산 |

→ **실질 고정비 = 도메인 값(연 1~2만원)뿐**입니다. 나머지는 계속 0원 유지.

---

## 지금 바로 시작할 수 있는 것 (요청 주시면 진행)

- **(개발) robots.txt + sitemap.xml 생성** — 3단계, 도메인 없이도 미리 가능
- **(개발) ads.txt 뼈대 준비** — 게시자 ID 받으면 바로 채움
- **(직접)** 도메인 후보 정하기 → 구매 (1단계)

원하시는 것부터 말씀해 주세요.
