# TODO — 국내주식 기술적 지표 스크리너

> 위에서 아래로 하나씩. 각 항목은 한 세션에 끝낼 수 있는 크기.
> 계획 전체 맥락은 [PLAN.md](PLAN.md) 참고. 👤 = 사용자가 직접 해야 하는 일.
> **새로 생각난 일은 아래 '대기열'에 적는다.** Phase 0~6은 지나온 기록이다.

---

# 대기열 (2026-07-31 기준)

## 지금 걸려 있는 것 — 애드센스 심사 중

2026-07-31 신청 접수. 결과는 메일, 며칠~2주.

**심사 중에 해도 되는 것**: 매일 데이터 갱신, 블로그 글 추가, 데이터 분석, 버그 수정
**미루는 것**: 종목 페이지 5,015개 구조 변경, 광고 위치·개수 변경, 페이지 대량 추가·삭제

기준은 "심사자가 본 사이트와 크게 달라지는가". 구글이 수정을 금지하지는 않는다.

## A. 지금 해도 되는 것 (사이트 안 건드림)

- [ ] **패턴 등급이 성과를 가르는지 검증** — 백테스트가 A/B/C를 한 덩어리로 측정 중이다.
  등급별로 갈라 t-검정. 갈리면 shape 점수가 쓸모 있다는 근거이고 "A등급만" 필터가
  정당해진다. 안 갈리면 shape는 장식이라는 뜻 — 그것대로 중요한 사실
- [ ] **스윙 파라미터 민감도 분석** — `SWING_MINOR_ATR_MULT`를 1.5/2.0/2.5로 돌려
  검출 수·등급 분포 변화 측정. 모든 패턴이 스윙에서 나오므로 여기가 진짜 지렛대인데
  2.0이 최적이라는 근거가 지금 없다
- [ ] **패턴 안정성 측정** — 어제 잡힌 패턴이 오늘 한 봉 더 들어왔을 때 남아 있나.
  아카이브가 있어 과거 시점 재현 가능. 사라지는 비율이 높으면 사용자 신뢰가 깨진다
- [ ] **미래 참조 감사** — `confirmed_at` 이후에만 돌파를 스캔한다는 규율이 코드 전체에서
  지켜지는지 자동 검증. 한 군데라도 깨지면 백테스트 28전략이 전부 거짓말이 된다
- [ ] 블로그 글 추가 (현재 11편) — 심사에도 유입에도 도움

## B. 심사 결과 나온 뒤

### B-1. 업종 분류 개편 (1·2번은 한 작업)

- [ ] **섹터를 17개 → 25~30개로 세분화 + 대형주 예외 목록**

  발단: 업종맵에서 삼성전자와 SK하이닉스가 다른 칸에 있다.

  원인은 우리 매핑 버그가 아니라 데이터 원본의 분류다. 통계청 표준산업분류가
  주력 제품 기준이라 삼성전자를 휴대폰 제조사로 본다.
  ```
  삼성전자   업종 "통신 및 방송 장비 제조업" → IT·전자
  SK하이닉스  업종 "반도체 제조업"          → 반도체
  ```

  ⚠️ **네이버 분류를 가져다 쓰지 않는다.** 네이버는 업종 79개에 삼성전자를
  반도체(170종목)에 넣어 감각에 맞지만, `finance.naver.com/robots.txt`가
  `User-agent: * / Disallow: /`이고 분류 자체가 FnGuide(WICS) 것이라 재배포
  문제가 따라온다. 2026-07 KRX 데이터를 약관 읽고 접었던 것과 같은 상황이다.

  대신 **결과가 비슷해지도록 우리 분류를 고친다**:
  - 네이버 79개를 참고해 25~30개로 세분화 (반도체 장비·디스플레이·게임·제약 분리 등)
  - `batch/sectors.py`에 대형주 예외 목록. 공식 업종이 실제 사업과 어긋나는 종목을
    손으로 지정한다. 근거는 공개된 사업 내용. 시총 상위 100종목이면 맵의 90%가 잡힌다
  - 초안은 코드가 만들고 **최종 판단은 사람이 한다** — 표로 뽑아 검토 후 확정

### B-2. 업종 지수 차트

- [ ] **묶인 업종을 차트로** (라인 최소, 캔들·패턴까지 가능)

  재료는 다 있다 — 시가총액 2,566종목 전부, 일봉 10년치.

  방법: 섹터별 시가총액 가중 지수. 현재 시총 ÷ 현재가로 주식수를 역산해 고정하고
  과거 종가에 곱해 더한다. 시가·고가·저가도 같은 방식으로 가중합하면 **캔들도 된다**.
  지수 시계열이 나오면 기존 파이프라인(RSI·MACD·볼린저·패턴·다이버전스)을 그대로
  태울 수 있다 — 종목 하나 늘어난 것처럼 취급.

  ⚠️ 화면에 밝혀야 할 한계 3가지:
  - 지수의 고가는 개별 종목 고가의 합이 아니다 (종목마다 고점 시각이 다름). 근사값이다
  - 주식수를 현재 값으로 고정하므로 과거 증자·분할이 반영되지 않는다.
    최근 1~2년은 정확하고 10년 전으로 갈수록 오차가 커진다
  - 신규 상장 종목은 상장 전 구간이 비어 지수가 튄다. 편입 시점 처리 필요

  B-1이 먼저다 (분류가 바뀌면 지수도 다시 만들어야 한다).

### B-3. 그 밖에

- [ ] **종목 페이지에 지표 표** — 지금 지표 값이 `<canvas>` 안에만 있어 종목 페이지의
  텍스트가 539자뿐이다(대부분 버튼 이름). 같은 값을 HTML 표로 한 벌 더 둔다.
  새로 계산할 건 없고 JSON에 있는 값을 꺼내는 일. 검색 유입 + 모바일 가독성 +
  스크린리더 대응. ⚠️ 자동 생성 문장("삼성전자는 현재 RSI 48.8로…")은 넣지 않는다 —
  틀만 같고 숫자만 바뀌는 문장 5,015개는 저품질로 읽힌다
- [ ] 👤 애드핏 광고 단위 3개 추가 (`320x50` 스티키가 모바일 최고 가치, `320x100`, `728x90`)
  → `AD_UNITS`에 연결
- [ ] 👤 증권사 계좌개설 CPA 제휴 (LinkPrice/Tenping). 금소법상 증권사 제공 소재만
  사용, 공정위 지침상 유료 링크 표시 필수

### B-3-2. 금리·매크로 차트

- [ ] **미국채 금리 등 매크로 계열 추가**

  이미 쓰는 FinanceDataReader로 바로 받아진다. 2026-07-31 실측:
  ```
  FRED:DGS30    미국채 30년      2,759행(10년)   5.21%
  FRED:DGS10    미국채 10년      2,759행         4.68%
  FRED:DGS2     미국채 2년       2,759행         4.23%
  FRED:T10Y2Y   10년-2년 스프레드 2,760행         0.47%p
  USD/KRW       원달러           2,763행         1,442원
  ```
  FRED는 미 연준의 공개 데이터라 이용 제약이 없다 (KRX·네이버와 다른 점).

  ⚠️ 금리는 주가와 성격이 다르다:
  - 시가·고가·저가가 없다. 종가 하나뿐이라 **캔들 불가, 라인만**
  - **패턴 탐지는 붙이지 않는다.** 쌍바닥·플래그는 수급과 심리가 만드는 형태인데
    국채 금리는 연준 정책과 물가가 움직인다. 그림은 그려지지만 의미가 없다.
    RSI·이동평균 정도는 추세 판단에 쓸 만하다

  단순 금리 차트만으로는 다른 사이트에도 있는 걸 하나 더 만드는 셈이다.
  **우리 데이터와 붙일 때** 값이 생긴다:
  - 금리 상승 구간에 어느 업종이 오르고 내렸는지 (은행 강세·성장주 약세라는 통설을
    국내 시장 실측으로 확인). 2,566종목 10년치가 있어 계산 가능하다
  - 장단기 금리차 역전 이후 코스피가 어땠는지 (백테스트 엔진 재활용)
  - 금리 급변일의 업종별 반응

  **B-2(업종 지수)가 먼저다** — 업종별 지수가 있어야 상관을 계산할 수 있다.
  개인 투자 판단에도, 블로그 소재로도 쓰인다.

## B-4. 애드센스 계정 정리 (진행 중, 2026-07-31~)

신청은 접수됐다가 **중복 계정으로 막혔다**. 애드센스는 1인 1계정이다.

현재 상태:
```
pub-8095107449407074   운영중. 애드몹 연결. 결제 프로필 = 개인 / JANG SEHOON
                       (계좌 유형은 '영구' 설정이라 개인→비즈니스 변경 불가)
pub-2039230424038768   중복으로 막힌 신청 건. 사이트 코드·ads.txt에 이게 박혀 있다
```

세훈님 방침: 8095를 **회사 전용 계정으로 새로 정리**하고, 개인 계정을 따로 살려
kscreener를 붙인다.

- [ ] 👤 회사 전용 계정 신설 → 앱 광고 단위 이전 → **마지막에** 기존 계정 정리
  ⚠️ 순서 중요: 애드몹 광고 단위 ID(`ca-app-pub-8095…/…`)에 게시자 ID가 박혀 있어
  계정을 먼저 해지하면 출시된 앱이 즉시 광고를 잃는다. 새 ID로 앱을 업데이트하고
  스토어 재배포가 끝난 뒤에 정리해야 공백이 없다. 해지는 되돌릴 수 없다
- [ ] 👤 해지 전 지급 이력·세금 자료 내려받기
- [ ] 👤 개인 계정으로 애드센스 → kscreener.com 사이트 추가 → 검토 요청
  ⚠️ 중복 판정이 또 뜰 수 있다 (구글은 이름·주소·전화·계좌를 종합해 본다).
  법인 사업자등록번호·법인 계좌로 회사 계정이 확실히 갈려 있어야 한다
- [ ] **최종 게시자 ID가 정해지면 사이트 코드·ads.txt 교체 후 배포** (10분)
  `web/app/layout.tsx` head 스크립트 + `web/public/ads.txt` 두 곳

그때까지 사이트는 그대로 둔다 — 애드핏은 정상 동작 중이고, 애드센스 태그가
맞지 않는 ID여도 광고가 안 나올 뿐 사이트에 문제를 일으키지 않는다.

## C. 검색 유입 (며칠~몇 주 뒤 확인)

- [ ] 서치콘솔에서 블로그 사이트맵이 "성공 / 21"로 바뀌었는지 (7/31 제출, 첫 크롤 대기)
- [ ] 서치콘솔 → 페이지 → **색인 생성됨** 숫자 확인. 블로그 글이 들어오기 시작하는지
- [ ] 네이버 서치어드바이저 최적화 보고서 한 번 돌려보기
- [ ] 👤 네이버 웹 페이지 수집 요청 마무리 (블로그 글 11개 중 남은 것)

## D. 판단 보류 중

- [ ] **유료 기능** — 연속 다이버전스는 실측상 2연속과 유의한 차이가 없어 근거가 안 된다.
  10년 차트는 이미 무료로 제공 중. 남은 후보는 "무료는 발견, 유료는 검증" 방향
  (종목별 시그널 성과 카드 등). 유사투자자문업 선을 넘지 않는 범위여야 한다
- [ ] **패턴 정답지** — shape 점수가 사람 눈과 맞는지 검증된 적이 없다. 200~300건을
  눈으로 ○/△/✗ 라벨링해 일치율 측정. 격자 뷰가 그대로 라벨링 도구가 된다
  (12개씩 훑기). 이게 없으면 어떤 파라미터 개선도 개선인지 알 수 없다

---

## Phase 0 — 준비

- [x] git 저장소 초기화, `.gitignore` (.env, 데이터 캐시, node_modules)
- [x] 👤 공공데이터포털 가입 + API 키 발급 → `.env`에 기입 완료 (`.env.example`에는 넣지 말 것 — 커밋되는 파일)
- [x] `.env.example` 작성
- [x] 디렉터리 골격 생성: `batch/`, `web/`, `.github/workflows/`

## Phase 1 — 배치 파이프라인

### 1-A. 수집

- [x] Python 프로젝트 셋업 (uv + Python 3.12, pandas/pykrx/FinanceDataReader)
- [x] 종목 마스터 수집: KOSPI+KOSDAQ 전 종목 (FinanceDataReader KRX 목록 — pykrx 목록 API는 빈 값 반환하여 대체)
- [x] 종목 필터 규칙: KONEX·스팩·우선주 제외 → 2,578종목
- [x] 백필 스크립트: 전 종목 최근 2년 일봉 → parquet 캐시, 이어받기 지원 (전 종목 완료, ~8분)
- [x] 공공 API 클라이언트 (`batch/collector/daily.py`) — 실호출 검증 완료 (2,730종목 수신)
- [x] 일별 수집: 공공 API 우선 + fdr 증분 폴백, 휴장일 처리 — 전체 파이프라인 완주 확인
- [x] 검증: 공공 API vs fdr 캐시 전 종목 대조 — 2,473종목 중 불일치 3건(0.1%)
- [x] 수정주가 정합성: 일별 병합 시 25% 이상 급변 감지 → 해당 종목 캐시 재구축(`daily.is_discontinuous`), `--rebuild-all` + 워크플로 토요일 크론으로 주 1회 전 종목 재백필. 30% 미만의 작은 조정은 급변 감지로 못 잡음 → 주간 재백필이 보정 (최대 1주 지연 허용)

### 1-B. 지표 계산

- [x] RSI(14) Wilder 방식 + 과매수/과매도 플래그
- [x] MACD(12,26,9) + 골든/데드크로스, 0선 돌파 플래그
- [x] 볼린저밴드(20,2) + 터치/%B/밴드폭/스퀴즈 플래그
- [x] 데이터 길이 부족 종목 스킵 (60봉 미만, 32종목 스킵됨)
- [x] 지표값 검증: 독립 구현(`ta` 라이브러리)과 대조 — MACD/BB 완전 일치, RSI 오차 <0.06
- [x] 지표 단위 테스트 (StockCharts 공식 예제값 대조 포함)

### 1-C. 다이버전스 (핵심)

- [x] 피벗 탐지 (좌우 lookback, `config.py`로 분리)
- [x] Regular/Hidden bullish/bearish 4종 판정
- [x] 판정 근거(피벗 날짜·값 쌍) 저장 → 차트 마킹에 사용
- [x] 합성 데이터 단위 테스트 (4종 + 경계 케이스)
- [ ] 실사례 백테스트: 알려진 다이버전스 사례 3건 이상으로 파라미터(PIVOT_LEFT/RIGHT, DIV_RECENT_BARS) 튜닝
- [ ] 삼성전자 차트 마킹 결과 눈으로 재검토 후 파라미터 확정

### 1-D. 산출

- [x] `signals/latest.json` (전 종목, 864KB)
- [x] `chart/{code}.json` (종목별 250일 + 다이버전스, 총 ~62MB)
- [x] `python -m batch.run` 진입점 (전 종목 계산 12초)
- [x] JSON 크기 확인

## Phase 2 — 웹 MVP

- [x] Next.js(App Router, TS) 셋업
- [x] 공통 레이아웃: 헤더/푸터, 전일 기준 고지, 면책 문구, 광고 슬롯 자리
- [x] 스크리너: 12종 플래그 체크박스(그룹핑) + AND 필터 + 결과 테이블 + 정렬
- [x] URL 동기화 `/screen?flags=...` + 동적 SEO 메타태그
- [x] 종목 상세 `/stock/[code]` SSG + lightweight-charts (캔들+BB, 거래량, RSI, MACD 4패널, 다이버전스 마커)
- [x] 지표 가이드 페이지 초안 (`/guide`)
- [x] 모바일 레이아웃 (테이블 가로 스크롤)
- [x] 종목 검색 (이름/코드) — 헤더 검색창 (자동완성, 키보드 내비게이션)
- [x] 주요 필터 조합 정적 페이지 8개 (`/screen/[slug]`) + sitemap/robots
- [x] 👤 Vercel 계정 연결 + 배포 — https://stock-screener-flame-omega.vercel.app

## Phase 3 — 자동화

- [x] GitHub Actions 워크플로 작성 (`daily-batch.yml`: KST 22시 + 익일 9시 재시도, 캐시, 신선도 검사, data 브랜치 push, Deploy Hook)
- [x] 👤 GitHub 저장소 생성 + push — github.com/DashHoon/stock-screener
- [x] 👤 GitHub Secrets 등록: `DATA_GO_KR_API_KEY`
- [x] 👤 Vercel 프로젝트 생성(Root Directory=`web`) + Production Branch를 `data`로
- [ ] Supabase 이력 적재 (선택 — MVP에는 불필요, 이력 API 필요해질 때):
  - [ ] 👤 Supabase 프로젝트 생성
  - [ ] `ohlcv`/`indicators` 테이블 DDL + 배치 upsert
- [x] workflow_dispatch로 수동 실행 검증 — 성공 (7분 30초, data 브랜치 → Vercel 자동 배포 → 사이트 반영 확인)
- [ ] 2~3일 연속 무개입 자동 갱신 확인 (평일 크론 KST 22시 — 첫 자동 실행은 오늘 밤)

## Phase 5 — 차트 v2 + 백테스팅 (상세: [CHART_PLAN.md](CHART_PLAN.md))

### C1. 차트 UX — 완료 (배포됨)

- [x] 차트 툴바 컴포넌트 (컨트롤 바) — 좁은 화면에서는 '차트 설정'으로 접힌다
- [x] 높이 조절 3단 토글 (기본 '크게', localStorage)
- [x] 기간 버튼 1주/1M/3M/6M/1Y/전체
- [x] 패널 라벨 "거래량/RSI/MACD" 표시
- [x] RSI 30·70 기준선 진하게
- [x] 이동평균선 5·20·60·120 + 범례
- [x] MACD 골든/데드크로스 ▲▼ 마커
- [x] 표시 토글: 다이버전스/MACD크로스/이평선/볼린저밴드/패턴/캔들
- [x] 모바일: 차트 위 광고 슬롯 (모바일 전용)
- [x] 모바일 핀치줌 수정 + 뷰포트 검증
- [x] 그리기 도구 (2026-07-30): 추세선·박스, 시작/종료 모드, 핸들로 길이·각도·크기 조절,
  (시간, 가격)으로 저장해 확대·이동해도 캔들에 붙어 있다

### C2. 주봉/월봉 + 데이터 확장

- [x] 주봉/월봉 리샘플 모듈 + 타임프레임별 지표·다이버전스 재계산 (테스트 23개)
- [x] chart JSON v2 `{tf:{d,w,m}}` — 있는 기간만큼 산출, 봉 부족 타임프레임 자동 생략
- [x] 프론트 일/주/월 탭 + 타임프레임별 기간 버튼 (브라우저 검증 완료)
- [x] 10년 백필 완료 (2026-07-22): 2,399/2,578종목 확보, 실패 0. 근본 원인 2건 해결 —
  requests 무한대기(httpguard 15s 강제) + 대용량 요청 조르기(2년 조각 분할·청크 쿨다운)
- [ ] CI 배포 검증: 다음 크론이 10년 캐시를 새로 구축하는지 (캐시 키 ohlcv-cache-10y-)

### C3. 백테스팅 v1 — 전략 성과 통계 (2026-07-22 배포)

- [x] 전략 엔진: trigger→when→confirm(기한·지표조건)→보유기간, 60일 중복 억제, 시장 기준선
- [x] 초기 전략 10종 (조합 전략 포함) → stats/backtest.json, 매일 갱신
- [x] /stats 페이지 (성과 표 + 생존편향·거래비용·면책 고지) + nav/sitemap
- [x] 엔진 단위 테스트 6종
- [ ] 종목 상세·프리셋 페이지에 관련 전략 통계 요약 삽입 (후속)
- [ ] 표본 수기 대조 검증 (특정 종목 1~2개 신호일·수익률 손으로 확인) (후속)
- [ ] v2(성장 시): 방문자 자유 조합 백테스트 — 유료(프리미엄) 후보, 법적 검토 필요

## Phase 6 — 차트 패턴 + 기간 필터 (상세: [PATTERN_PLAN.md](PATTERN_PLAN.md))

### P1. 스크리너 기간 필터 — 완료 (2026-07-22 배포)

- [x] latest.json 개편: flags → sig(마지막 발생 N봉 전, 63봉 상한) — 크기 864→526KB
- [x] recent.py 계산 모듈 + 테스트 4종
- [x] 스크리너 기간 선택(오늘/1주/1M/3M) + URL within + 배지 경과일
- [x] 웹·데이터 동시 배포, 라이브 확인

### P2. 패턴 엔진 + 쌍바닥/더블탑 — 완료 (2026-07-23 배포)

- [x] 쌍바닥/더블탑 탐지 (완성=넥라인 돌파일 / 형성중 / 무효화 구분, 테스트 5종)
- [x] 차트 마킹 (지그재그+넥라인+마커) → KB금융 실사례 육안 검증
- [x] 스크리너 '차트 패턴' 그룹 4칩 + 기간 필터 적용
- [x] 백테스트 전략 등록 (쌍바닥 돌파 42.2% 승률 — 기준선 하회. 거래량 확인 등
  필터 보강 여지, 통계 페이지에 투명하게 노출됨)
- [x] 배포
- [ ] (후속) 쌍바닥 품질 필터 실험: 돌파일 거래량 조건, 선행 하락폭 조건 등
- [ ] (알려진 한계) 저가가 여러 봉 동률인 평평한 바닥은 피벗 미검출

### 패턴 순차 추가 — 우선순위 변경(2026-07-23): 어려운 패턴 먼저

- [x] **컵앤핸들** (2026-07-23 배포): U자 검증(바닥권 체류+포물선 적합) + 핸들 + 돌파.
  종목당 연평균 0.33건, 완성 44·형성 17종목. 거래정지 종목 시그널 제외 수정 포함.
  ⚠️ 10년 백테스트: 돌파일 진입 기준 시장 대비 열위(-0.3~-0.7%p) — 거래량 확인,
  추세 맥락 등 품질 필터로 개선 여지 (쌍바닥도 유사). 후속 튜닝 소재
- [x] 라운드 바텀/탑 (2026-07-23) — 컵 곡선 검증 재활용, 림 돌파 완성
- [x] 다이아몬드 탑, 브로드닝 탑 (2026-07-23) — 확대/수렴 폭 검증
- [x] 헤드앤숄더/역H&S + 3중바닥/트리플탑 (2026-07-23) — 피벗 시퀀스 + 넥라인
- [x] 플래그/페넌트, 삼각형 3종 + 쐐기 2종 (2026-07-23) — 깃대+조정 / 피벗 추세선 분류
- [ ] 패턴 품질 필터 실험: 돌파일 거래량·선행 추세 조건으로 백테스트 성과 개선
  (현재 돌파 전략들이 시장 대비 열위 — 모든 패턴 공통의 개선 소재)
- [ ] 배치 소요 증가 관찰: 패턴 스캔 추가로 재산출 ~6분 + 백테스트 ~5분 (CI에서 확인)

## Phase 4 — 공개 준비

- [x] 콘텐츠 페이지: 지표 가이드 3편 + 블로그 11편 (`/blog`, 편당 5,100~10,800자)
- [x] SEO: sitemap.xml, robots.txt, OG 이미지
- [x] 소개·문의 페이지 (2026-07-31) — 메인 도메인. 그전까지 블로그에만 있었다
- [x] 👤 카카오 애드핏 → `DAN-44QtS6vWPeIcgL5N` (300x250) 연결. 단위 3개 추가는 대기열 B
- [ ] 👤 증권사 계좌개설 CPA 제휴 신청 → 배너 적용 (대기열 B)
- [x] 👤 도메인 구매 + Vercel 연결 — kscreener.com
- [x] 면책·지연시세 고지 최종 점검, 공개
- [x] 👤 애드센스 신청 (2026-07-31 접수, 심사 중) — `ca-pub-2039230424038768`,
  ads.txt 등록, 구글 CMP 3버튼(거부 포함) 채택
