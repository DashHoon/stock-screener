# kscreener 앱 기획

> 2026-08-22 작성. 운영자 구술을 정리한 것이다.
> 기술 선택·수익화는 [APP_PLAN.md](APP_PLAN.md), [APP_MONETIZE.md](APP_MONETIZE.md).

---

## 한 줄로

**차트 패턴으로 종목을 찾는 앱.** 다른 기능은 이 검색을 돕기 위해 있다.

---

## 1. 검색 — 앱의 전부

### 1-1. 무엇으로 찾나 (5가지)

| | 검색 대상 | 현재 상태 |
|---|---|---|
| **1** | **차트 패턴** — 쌍바닥·플래그·삼각수렴 등 21종 | 있음 |
| 2 | 캔들 패턴 — 망치·유성·적삼병 등 18종 | 있음 |
| 3 | RSI — 과매수/과매도, 다이버전스 4종, 연속 다이버전스 4종 | 있음 |
| 4 | MACD — 골든/데드크로스, 0선 돌파 | 있음 |
| 5 | **이격도** — 종가가 이동평균에서 얼마나 떨어졌나 | **없음. 새로 만들어야 함** |

**1번이 마케팅의 중심이다.** 캔들·RSI·MACD는 다른 앱에도 있지만, 차트 패턴을
전 종목에서 자동으로 찾아주는 앱은 드물다. 앱 소개·스크린샷·블로그 글이 전부
여기를 가리켜야 한다.

> **이격도는 새 작업이다.** 종가 ÷ N일 이동평균 × 100. 20일·60일 기준이 흔하고,
> "20일선 대비 +15% 이상" 같은 범위 검색이 된다. 배치에 지표 하나를 추가하고
> 스크리너 조건에 범위 입력을 넣는 일이라 크지 않다.

### 1-2. 조합

위 5가지를 **AND로 겹쳐서** 찾는다. 예: `상승 플래그 + 상승 다이버전스 + 20일 이격도 -10% 이하`

### 1-3. 좁히는 축 (지금 웹과 동일)

- **발생 시점** — 당일 / 1주 / 1개월 / 3개월 안에 발생
- **시가총액** — 하한 지정
- **시장** — 코스피 / 코스닥 / 전체
- **방향** — 상승 / 하락 시그널

### 1-4. 정렬

- 시가총액 순
- 등락률 순

---

## 2. 이 앱의 약점 두 가지 — 숨기지 않고 설계에 넣는다

### 약점 1. 패턴은 100% 정확할 수 없다

차트 패턴은 보는 사람마다 다르게 읽는다. 알고리즘도 계속 고쳐 나가는 중이다.

**어떻게 다룰까**
- 앱 안에서 **형태 신뢰도 등급(A/B/C)** 을 그대로 보여준다. 이미 계산하고 있다
- "이 패턴이 맞다"가 아니라 "이런 모양으로 잡혔다"는 말투를 쓴다
- 사용자가 직접 차트를 보고 판단하도록, 검색 결과에서 **차트로 바로 넘어가는
  동선**을 가장 짧게 만든다 (3장 참고)
- 오탐 제보를 받는 통로를 둔다 — 알고리즘 개선의 재료가 된다

절대 하지 않을 것: 정확도·승률을 숫자로 내세워 파는 것. 그 숫자를 우리가 아직
믿지 못한다(`marketing/off-limits.md` 1항).

### 약점 2. 실시간이 아니다 — 1영업일 지연

**정면으로 설명한다.** 숨기면 사용자가 배신감을 느끼고, 설명하면 납득한다.

설명 논리:
> 추세 지표는 원래 후행한다. 패턴은 완성되기 전에 형태가 먼저 만들어지고,
> 매수·매도 판단은 그 뒤 1~2일에 걸쳐 하게 된다. 하루 지연 데이터로도
> **찾는 일**은 충분히 되고, 실행은 사용자가 실시간 시세를 보며 한다.

**보완책 — 형성 중 패턴 검색**

완성된 패턴만 보면 이미 늦는다. 그래서 **아직 만들어지는 중인 패턴**도 찾을 수
있게 한다. 이미 데이터가 있다 (형성 중 19종, 예: 상승 플래그 형성 중 346종목).

```
완성   목선을 이미 돌파했다        → 확인됐지만 늦을 수 있다
형성 중 아직 만들어지는 중이다      → 미리 보되 무산될 수 있다
```

이 구분을 화면에서 뚜렷하게 보여준다. 형성 중은 "확정 아님"을 배지로 단다.

---

## 3. 차트 보기

### 3-1. 가로 보기 지원

세로만으로는 차트가 답답하다. **기본은 세로, 원할 때 가로로 전환**한다.
가로로 돌리면 차트가 화면을 꽉 채우고 목록·헤더는 숨는다.

### 3-2. 위아래로 넘기며 다음 종목

검색 결과에서 한 종목을 열었을 때, **위아래 스크롤로 다음·이전 종목**으로 바로
넘어간다. 목록으로 돌아갔다 다시 들어오는 왕복이 없다.

> 이게 이 앱의 사용감을 가르는 지점이다. 패턴 검색은 결과가 수십 개씩 나오고,
> 사용자는 그걸 눈으로 훑어 걸러낸다. 한 종목 보는 데 두 번 탭해야 하면
> 30개를 못 본다.

---

## 4. 관심종목 — 그룹으로 나눈다

하나의 긴 목록이 아니라 **사용자가 만든 그룹**으로 나눈다.

```
보유 종목    삼성전자 · SK하이닉스 · …
반도체       한미반도체 · 원익IPS · …
바이오       …
관심         …
```

- 그룹을 누르면 **검색 결과와 같은 화면**으로 보여준다 (같은 카드·같은 정렬·같은 차트 동선)
- 즉 그룹은 "저장된 검색 결과"처럼 동작한다. 화면을 새로 만들지 않는다

---

## 5. 푸시 알림 — 조건을 걸어두면 알려준다

사용자가 조건을 미리 지정해 두면, 그 조건이 **완성됐을 때** 알림을 보낸다.

```
예)  시가총액 5,000억 이상
   + 상승 플래그 형성
   + 상승 다이버전스
   → 조건에 맞는 종목이 나오면 푸시
```

**동작 시점**: 매일 배치가 끝난 뒤(오후) 한 번. 실시간이 아니다 — 데이터 자체가
하루 지연이라 실시간 알림은 의미가 없다.

**무료로 열지, 구독 전용으로 할지**
- 원칙: **최대한 무료 범위에서** 만든다
- 다만 알림은 이 앱에서 **서버 비용이 실제로 드는 유일한 기능**이다
  (사용자별 조건 저장 + 토큰 관리 + 발송)
- 그래서 이렇게 시작한다: **무료 2개까지, 구독은 무제한**
- 비용이 감당 가능하면 무료 한도를 늘리고, 부담되면 줄인다. 숫자는 나중에 조정

---

## 6. 지금 있는 것 / 새로 만들 것

| | 상태 |
|---|---|
| 차트 패턴 21종·캔들 18종·RSI·MACD | **있음** — 배치가 매일 계산 중 |
| 형성 중 패턴 19종 | **있음** |
| 발생 시점·시총·시장·방향 필터, 정렬 | **있음** (웹 스크리너와 동일 로직) |
| 종목 차트 데이터 (일·주·월, 10년) | **있음** |
| 회사 개요·뉴스·업종 지수 | **있음** |
| **이격도** | 새로 — 배치에 지표 추가 + 범위 검색 |
| **가로 보기** | 새로 — 앱 화면 |
| **상하 스와이프로 종목 이동** | 새로 — 앱 화면 |
| **관심종목 그룹** | 새로 — 앱 로컬 저장 |
| **푸시 알림** | 새로 — **서버가 필요한 유일한 기능** |

데이터는 이미 다 있다. 앱이 할 일은 **기존 JSON을 읽어 화면을 만드는 것**이고,
서버가 필요한 건 푸시 하나뿐이다.

---

## 7. 만드는 순서

- [x] **0. 기술 선택 — Flutter** (2026-08-22). 차트는 CustomPainter로 직접 그린다
- [ ] 1. 검색 화면 + 결과 목록 (기존 JSON 그대로)
- [ ] 2. 차트 상세 — 가로 보기, 상하 스와이프
- [ ] 3. 관심종목 그룹 (로컬 저장)
- [ ] 4. 이격도 추가 (배치 + 검색 조건)
- [ ] 5. 광고 붙이기 ([APP_MONETIZE.md](APP_MONETIZE.md))
- [ ] 6. 푸시 알림 (서버 설계 필요)
- [ ] 7. 스토어 등록

**푸시를 뒤로 둔 이유**: 유일하게 서버가 필요해서 가장 복잡한데, 없어도 앱은
성립한다. 앞의 것들로 먼저 내고 사용자가 붙는지 본 다음에 만든다.

---

## 하지 않을 것

- 정확도·승률을 내세운 마케팅 — 숫자를 아직 믿지 못한다
- 종목 추천·목표가 — 유사투자자문업
- 실시간 시세인 척하기 — 하루 지연을 화면에 항상 표시한다
