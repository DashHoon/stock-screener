# 트래픽 분석 설치 가이드 (GA4 + 서치 콘솔)

사이트 방문자와 검색 유입을 측정하기 위한 **Google Analytics 4**와 **Google Search Console** 설정 순서입니다.

> **코드 작업은 이미 끝나 있습니다.** 두 도구 모두 "환경변수에 값이 있으면 켜지고, 없으면 아예 안 붙는" 구조로 심어두었습니다.
> 그래서 사용자님이 하실 일은 **구글에서 값 2개를 받아 Vercel에 붙여넣고 재배포**하는 것뿐입니다.
> 계정 생성·로그인은 제가 대신 못 하므로 이 부분만 직접 해주셔야 합니다.

| 넣을 값 | 환경변수 이름 | 어디서 받나 |
|---|---|---|
| 측정 ID (`G-`로 시작) | `NEXT_PUBLIC_GA_ID` | Google Analytics |
| 소유 확인 문자열 | `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` | Google Search Console |

---

## 1단계 · Google Analytics 4 속성 만들기

### 왜 하나
방문자 수, 어떤 페이지가 인기 있는지, 어디서 유입되는지를 봅니다. 나중에 애드센스 계정과 연결하면 **페이지별 광고 수익**까지 한 화면에서 볼 수 있습니다.

### 할 일

1. https://analytics.google.com 접속 → 구글 계정으로 로그인
2. 처음이면 **측정 시작**, 이미 계정이 있으면 좌하단 **관리(톱니바퀴)** → **만들기** → **속성**
3. 계정 이름: 아무거나 (예: `kscreener`)
4. 속성 설정
   - 속성 이름: `kscreener`
   - 보고 시간대: **대한민국**
   - 통화: **대한민국 원(₩)**
5. 비즈니스 정보: 업종은 `금융`, 규모는 `작음` 정도로 선택 (통계에 영향 없음)
6. 비즈니스 목표: **기준 보고서 보기** 선택 (가장 무난)
7. 데이터 수집 → 플랫폼에서 **웹** 선택
   - 웹사이트 URL: `https://kscreener.com`
   - 스트림 이름: `kscreener`
   - **향상된 측정**은 **켜진 상태 그대로** 두세요 → 이게 켜져 있어야 페이지 이동이 자동 집계됩니다
8. 완료하면 오른쪽 위에 **측정 ID**가 뜹니다 → `G-XXXXXXXXXX` 형태
9. **이 값을 복사해 두세요.**

> 다음 화면에서 "코드를 직접 설치" 같은 스니펫을 보여주는데, **복사해서 어디에 붙이실 필요 없습니다.** 이미 코드에 들어가 있고 측정 ID만 있으면 됩니다.

- [ ] 측정 ID 확보 (`G-__________`)

---

## 2단계 · Google Search Console 등록

### 왜 하나
GA는 **들어온 사람**만 보여주지만, 서치 콘솔은 **아직 안 들어온 사람**을 보여줍니다 — 어떤 검색어로 몇 번 노출됐는데 클릭이 안 됐는지가 나옵니다. 광고 수익이 검색 유입에 달린 이 사이트에서는 이게 사실 GA보다 중요합니다. 색인 누락(구글이 페이지를 못 찾는 상태)도 여기서만 확인됩니다.

### 할 일

1. https://search.google.com/search-console 접속 → **1단계와 같은 구글 계정**으로 로그인
2. 속성 유형 선택 화면에서 **오른쪽의 `URL 접두어`** 를 고르세요
   - 왼쪽 `도메인`은 DNS 설정이 필요해서 번거롭습니다. 오른쪽이 간단합니다.
3. `https://kscreener.com` 입력 → 계속
4. 소유권 확인 방법 중 **`HTML 태그`** 를 펼칩니다
5. 이런 태그가 보입니다:
   ```html
   <meta name="google-site-verification" content="abcd1234EFGH..." />
   ```
   여기서 **`content="..."` 안의 값만** 복사하세요. `abcd1234EFGH...` 부분입니다.
   태그 전체를 복사하면 안 됩니다.
6. **아직 "확인" 버튼을 누르지 마세요.** 3단계에서 값을 넣고 배포한 뒤에 눌러야 통과합니다.

- [ ] 소유 확인 값 확보 (`content=` 안의 문자열)

---

## 3단계 · Vercel에 값 넣고 재배포

1. https://vercel.com 로그인 → `stock-screener` 프로젝트 클릭
2. 상단 **Settings** → 왼쪽 메뉴 **Environment Variables**
3. 아래 2개를 추가합니다. 각각 **Add** 를 눌러 하나씩 넣으세요.

   | Key | Value | Environments |
   |---|---|---|
   | `NEXT_PUBLIC_GA_ID` | 1단계에서 받은 `G-XXXXXXXXXX` | **Production만 체크** |
   | `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` | 2단계에서 받은 `content` 값 | Production, Preview, Development 전부 |

   > **GA를 Production만 체크하는 이유**: 프리뷰 배포나 로컬 개발에서도 켜지면 제 테스트 방문이 통계에 섞여 방문자 수가 부풀려집니다. 실제 사이트에서만 켜지게 해두는 편이 데이터가 깨끗합니다.

4. **재배포** — 환경변수는 다음 배포부터 적용되므로 반드시 한 번 다시 배포해야 합니다.
   - Vercel 상단 **Deployments** 탭 → 맨 위 배포의 우측 `⋯` → **Redeploy** → 확인
   - 또는 밤 크론이 돌면서 자동으로 반영되니 급하지 않으면 다음 날 확인해도 됩니다

- [ ] 환경변수 2개 등록
- [ ] 재배포 완료

---

## 4단계 · 제대로 붙었는지 확인

재배포가 끝난 뒤(보통 2~4분) 확인합니다.

### 서치 콘솔 소유 확인
1. 2단계에서 열어둔 서치 콘솔 화면으로 돌아가 **확인** 버튼 클릭
2. "소유권이 확인되었습니다"가 뜨면 성공
3. 실패하면 재배포가 아직 안 끝났을 가능성이 큽니다. 2~3분 뒤 다시 시도하세요.

### GA 작동 확인
1. 새 탭에서 https://kscreener.com 을 열고 페이지 몇 개를 눌러봅니다
2. GA 화면 왼쪽 **보고서** → **실시간**
3. "지난 30분 동안의 사용자"에 **1**이 잡히면 성공

### 터미널로 확인 (선택)
아래를 실행해 값이 보이면 정상입니다.

```bash
curl -s https://kscreener.com | grep -oE 'gtag/js\?id=G-[A-Z0-9]+|google-site-verification" content="[^"]+"'
```

- [ ] 서치 콘솔 소유권 확인됨
- [ ] GA 실시간에 내 방문 잡힘

---

## 5단계 · 사이트맵 제출 (서치 콘솔 확인 직후)

색인 속도를 크게 앞당기는 단계라 꼭 하세요.

1. 서치 콘솔 좌측 메뉴 **Sitemaps**
2. "새 사이트맵 추가"에 `sitemap.xml` 입력 → 제출
3. 상태가 "성공"으로 바뀌면 끝 (반영까지 몇 시간~며칠)

> 이 사이트는 종목 페이지가 2,500개가 넘습니다. 사이트맵을 제출해야 구글이 이걸 다 찾아갑니다. 제출 안 하면 색인이 아주 느립니다.

- [ ] 사이트맵 제출

---

## 자주 겪는 문제

**"소유권 확인 실패"가 계속 나옵니다**
`content="..."` 안의 값만 넣으셨는지 확인하세요. `<meta name=...>` 태그 전체를 넣으면 실패합니다. 그리고 값을 넣은 뒤 **재배포**를 했는지도 확인하세요 — 환경변수만 저장하면 사이트에는 아직 반영되지 않습니다.

**GA 실시간에 아무것도 안 잡힙니다**
- 광고 차단 확장 프로그램(uBlock, AdGuard 등)이 GA를 막습니다. 시크릿 창에서 확장 없이 접속해 보세요.
- 환경변수를 Production에 체크했는지 확인하세요.
- 측정 ID가 `G-`로 시작하는지 확인하세요. `UA-`로 시작하면 옛 버전이라 작동하지 않고, `GTM-`은 태그 매니저용이라 다릅니다.

**데이터가 하루 뒤에야 보입니다**
정상입니다. 실시간 보고서만 즉시 뜨고, 일반 보고서 집계는 24~48시간 걸립니다.

---

## 나중에 (지금은 안 해도 되는 것)

- **GA ↔ 서치 콘솔 연결**: GA 관리 → 서비스 간 연결 → Search Console 링크. 검색어 데이터를 GA 안에서 같이 봅니다.
- **GA ↔ 애드센스 연결**: 애드센스 승인 후에. 페이지별 수익이 GA에 표시됩니다.
- **Vercel Web Analytics**: GA와 겹치므로 지금은 불필요. Hobby 플랜은 월 5만 이벤트 한도가 있습니다.

---

## 개발자 참고 (코드 위치)

| 무엇 | 파일 |
|---|---|
| GA4 스크립트 (env 없으면 `null` 반환) | `web/components/Analytics.tsx` |
| 레이아웃 삽입 + 소유확인 메타태그 | `web/app/layout.tsx` |
| 개인정보처리방침의 GA 고지 | `web/app/privacy/page.tsx` §4 |
| 환경변수 목록 | `.env.example` |

로컬에서 테스트하려면 `web/.env.local`에 두 값을 넣고 `npm run dev`. 이 파일은 `.gitignore`에 있어 커밋되지 않습니다.
