# 차트 패턴 인식 + 기간 필터 계획서

> 요구 접수: 2026-07-22 · 상태: **전 패턴 배포 완료 (2026-07-23)** — 19종 시그널
> (쌍바닥·더블탑·컵앤핸들·H&S·역H&S·3중바닥·트리플탑·라운드바텀/탑·삼각형3종·
> 쐐기2종·플래그2종·브로드닝·다이아몬드) + 형성중 상태. 남은 것: 품질 필터 실험.
> 상위 문서: [PLAN.md](PLAN.md) · [CHART_PLAN.md](CHART_PLAN.md) · 체크리스트: [TODO.md](TODO.md) Phase 6

## 1. 요구사항

1. 대표 차트 패턴(쌍바닥, 헤드앤숄더, 삼각형, 플래그, 컵앤핸들 등)을 자동 탐지
2. **하나의 패턴씩 차근차근** 추가 (패턴별 검증·튜닝 사이클)
3. **기간 필터**: "3개월 이내 발생 / 1개월 이내 발생"처럼 기간을 정해 검색
4. 기간 필터는 패턴만이 아니라 **기존 12개 지표 시그널에도** 적용 (스크리너 공통 기능)

## 2. 탐지 원리

> **2026-07-28 방법론 교체**: 고정 lookback 피벗은 종목 변동성과 무관하게 잔파동을
> 피벗으로 잡고(엉뚱한 고점·저점 연결), 패턴 기간이 고정 상수로 결정되는 문제가
> 있었다. 이를 **ATR 적응형 ZigZag 스윙 구조**(`batch/patterns/swing.py`)로 교체 —
> 반전 폭이 max(k×ATR14, p%)를 넘는 극점만 스윙으로 인정하고(minor: 2×ATR/3%,
> major: 4×ATR/6% 2스케일), 패턴은 **연속 스윙의 배열**로 판정한다. 추세선은
> 회귀가 아니라 앵커 2점+터치 최대화로 긋고, 구조 범위는 두 선이 실제로 받쳐지는
> 지점부터로 트리밍한다. 추세선·쌍바닥·5스윙 계열의 돌파 스캔은 마지막 구조
> 스윙의 확정봉(confirmed_at = 반전 임계 충족봉)부터 시작해 미래 참조가 없다.
> 컵(핸들 최소 길이가 확정 지연 역할)·라운드(림 재접근봉부터)·다이아몬드(구간 끝
> 도달로 확정)는 각자의 구조 확정 시점부터 스캔한다 — 각 모듈 docstring 참고.
> (플래그는 봉 단위 깃대 로직 유지)

| 패턴 계열 | 판정 방법 |
|---|---|
| 쌍바닥/더블탑 | 연속 스윙 3개(저-고-저/고-저-고) 가격 유사성 + 넥라인 돌파 (minor·major) |
| 3중바닥/트리플탑, H&S/역H&S | 연속 스윙 5개 시퀀스 + 넥라인 돌파 (minor·major) |
| 삼각형 3종, 쐐기 2종, 브로드닝 | 스윙 고점열·저점열 앵커·터치 추세선 → 기울기 조합 + 안착률·지지 게이트 + 연쇄 억제 |
| 플래그/페넌트 | 직전 급등락(깃대) + 짧은 역방향 채널/수렴 (봉 단위, 스윙 미사용) |
| 컵앤핸들, 라운드 바텀/탑 | 림 = 스윙 고점(컵: minor∪major, 라운드: major) + 곡선(포물선) 적합 |
| 다이아몬드 | head = 스윙 고점 + 변동폭 확대→수렴 시퀀스 |

원칙:
- **"완성(돌파 확정)" vs "형성 중"을 구분**해 표기 (정직성)
- 패턴별로 차트에 **구성 라인을 마킹**해 눈으로 검증하며 파라미터 튜닝 (다이버전스 존 필터와 같은 사이클)
- 각 패턴은 **백테스트 엔진에 자동 연결**해 10년 성과 통계 검증 (우리만의 차별점)
- 오탐/미탐이 있는 영역임을 UI에 고지 (참고 정보)

## 3. 단계 로드맵

| 단계 | 내용 | 난이도 |
|---|---|---|
| **P1** | 스크리너 기간 필터 — 기존 12개 시그널을 "최근 N봉 내 발생"으로 개편 (기반 공사) | - |
| **P2** | 패턴 엔진 기초 + **쌍바닥/더블탑** (탐지→차트 마킹→튜닝→스크리너→백테스트) | 쉬움 |
| P3 | 삼각형 3종(상승/하락/수렴) + 쐐기 2종 | 중 |
| P4 | 헤드앤숄더/역H&S, 3중바닥/트리플탑 | 중 |
| P5 | 플래그/페넌트 | 중상 |
| P6 | 컵앤핸들, 라운드 바텀/탑, 다이아몬드, 브로드닝 | 어려움 |

## 4. P1 설계 — 기간 필터

**데이터 구조 변경** (`signals/latest.json`):
- 기존: `flags: {macd_golden: true, ...}` (당일 여부)
- 변경: `sig: {macd_golden: 3, ...}` (**몇 봉 전** 마지막 발생, 0=오늘, 63봉(~3개월) 초과는 생략)
- 이벤트형(다이버전스4·MACD3)은 발생일 기준, 상태형(RSI 과열/과매도, BB 터치/스퀴즈)은 마지막으로 조건을 만족한 날 기준

**스크리너 UI**:
- 기간 선택: 오늘 / 1주(5봉) / 1개월(21봉) / 3개월(63봉) — 기본 1주
- 필터 = 선택한 시그널들이 **모두 기간 안에 발생**(AND)
- URL 공유: `/screen?flags=...&within=21`
- 배지에 경과일 표시 (예: "골든 ·3일")

**배포 주의**: 데이터 포맷이 바뀌므로 웹·데이터 동시 배포 필요 (data 브랜치 일괄 push).

## 5. 품질 필터 실험 결과 (2026-07-23, 10년 × 전 종목/대형주 비교)

- **전 종목**: 거래량·추세 필터가 무효/역효과 (쌍바닥 60일 시장대비 -0.4 → 필터 후에도 ±0.3%p 내)
- **대형주(시총 상위 300)**: 동일 필터가 뚜렷하게 유효
  - 상승 플래그 + 거래량1.5x + 추세위: 시장대비 **+7.7%p** (60일, 승률 55.5%)
  - 컵앤핸들 + 거래량2.5x: +4.5%p (57.8%) / 역H&S + 거래량2.5x: +2.2%p
  - 추세 아래 진입은 대부분 큰 손해 (컵앤핸들 -8%p)
- **역발상 발견**: 상승 플래그를 장기추세 아래에서 잡으면 전 종목에서도 +4.2%p
  (하락장 반등 국면 특성일 수 있어 전략명에 주의 문구)
- 반영: 엔진에 universe(large300) 지원, [대형주] 전략 4종 + 역발상 1종 공개.
  교훈: **패턴 신호의 신뢰도는 유니버스(대형/소형)에 강하게 의존** — 이후 모든
  전략 검증에 유니버스 구분을 기본으로 한다.

## 6. 차후: 패턴 공통 스키마 (P2에서 확정)

패턴 탐지 결과는 지표 시그널과 같은 방식으로 스크리너에 편입:
- `sig`에 `pat_double_bottom: 5` 형태로 추가 → 기간 필터 그대로 적용
- 차트 JSON에 패턴 구성 요소(피벗 좌표, 넥라인) 추가 → 차트에 라인 마킹
- 백테스트 이벤트로도 등록 → /stats에 패턴 전략 통계 자동 노출
