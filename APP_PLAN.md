# 모바일 앱 전환 계획

> 2026-08-18 결정. 웹서비스를 **대체하는 것이 아니라 더한다** — 검색 유입이
> 웹에서 나오고 있어(클릭 5, 노출 203, 96페이지 노출) 웹은 그대로 둔다.

## 왜 앱인가 (운영자 지시, 네 가지 다 해당)

1. **수익** — 애드센스가 사이트를 '가치 없는 콘텐츠'로 반려했다(2026-08-13).
   AdMob 계정은 이미 있고 지급 이력도 있다. 앱은 그 경로를 그대로 쓴다.
2. **알림·관심종목** — 조건 발생 시 푸시. 매일 다시 오게 만드는 기능이고,
   앱스토어 심사가 요구하는 '웹에 없는 것'이기도 하다.
3. **사용성·속도**
4. **앱스토어 노출**

## 유리한 조건

데이터층이 이미 앱용 API나 마찬가지다. 정적 JSON을 CDN에서 받는 구조라
앱이 같은 주소를 그대로 부르면 된다. **서버를 새로 만들 필요가 없다.**

```
https://kscreener.com/data/signals/latest.json    1.5MB  전 종목 스크리닝
https://kscreener.com/data/chart/{code}.json      93KB   종목별 (일·주·월)
https://kscreener.com/data/chart/{code}.arc.json         과거 일봉
https://kscreener.com/data/chart/mini/{code}.json 2KB    격자용
https://kscreener.com/data/chart/sector/{slug}.json      업종 지수 34개
https://kscreener.com/data/stats/backtest.json           전략 통계
```

재사용 가능한 코드: `web/lib/flags.ts`(시그널 정의·라벨), `types.ts`,
`sectors.ts`(섹터·슬러그). 전부 순수 TypeScript라 그대로 옮긴다.

## 걸림돌

**차트.** lightweight-charts는 웹 전용이라 그대로 못 쓴다. 캔들 위에 패턴선·
넥라인·다이버전스 마커를 얹는 게 이 제품의 정체성이므로, 임의의 선을 그릴 수
있어야 한다.

Flutter의 `CustomPainter`가 그 일에 맞는다 — 캔버스에 직접 그리는 표준 방식이라
라이브러리가 지원하는 것만 그릴 수 있는 제약이 없다. 지금 웹에서
`ChartDrawings.tsx`가 (시간, 가격) → 화면 좌표로 변환해 그리는 구조를 그대로
옮기면 된다.

캔들 부분은 기성품에 얹어도 된다: `kline_chart`(MA·BOLL·MACD·RSI 내장,
확대·크로스헤어), `interactive_chart`(핀치 줌·패닝). 마킹이 많아지면 결국
CustomPainter로 수렴할 가능성이 높다.

**앱스토어 심사.** 웹사이트를 그대로 감싼 앱은 애플이 반려한다
(App Store Review 4.2 최소 기능). 알림·관심종목·위젯 같은 네이티브 기능이
실제로 있어야 한다.

**금융 앱 심사.** 투자 정보 앱은 면책 고지와 '투자 자문이 아님'을 명확히
해야 한다. 웹에 이미 있는 문구를 앱에도 그대로 싣는다.

## 성능 설계 — 어디가 실제로 느린가

운영자 지시: 성능을 최우선으로. 그렇다면 **차트 그리기부터 최적화하는 건 잘못된
출발**이다. 실측해 보면 병목이 다른 데 있다.

```
latest.json (검색용 전 종목)   1.8MB   2,565종목
chart/{code}.json (종목 1개)   95KB    일 392 · 주 520 · 월 120봉
mini/{code}.json               2.1KB
```

### 병목 1 — 검색 데이터 1.8MB (가장 큼)

앱을 켤 때마다 1.8MB를 받아 파싱하면 눈에 띄게 느리다. 맥북에서 12ms인데
중급 안드로이드는 3~5배다. 게다가 네트워크로 매번 받으면 그게 더 크다.

**대책**
- 첫 실행 때 받아 **로컬 DB(SQLite/Isar/Drift)에 넣고**, 이후에는 DB에서 읽는다.
  검색·정렬을 DB 쿼리로 넘기면 2,565건을 메모리에 올릴 일도 없다
- 갱신은 **날짜 비교로 판단**한다. `latest.json`의 `date`만 먼저 확인하고
  (HEAD 요청이나 작은 메타 파일), 같으면 통째로 받지 않는다
- 종목당 `m` 필드(스파크라인 20봉)가 전체의 **16%** 를 차지한다. 목록에
  스파크라인을 안 그린다면 배치에서 빼면 1.5MB로 준다

> ⚠️ 지금 구조는 앱을 고려하지 않고 만들어졌다. 필요하면 **앱 전용 경량 파일**을
> 배치에서 따로 내는 게 낫다 — 웹과 형식을 억지로 공유할 이유가 없다.

### 병목 2 — 상하 스와이프

기획의 핵심 동선이다(APP_SPEC 3-2). 종목을 넘길 때마다 95KB를 새로 받으면
스와이프가 걸린다.

**대책**
- **미리 받기(prefetch)** — 현재 종목을 보여주는 동안 앞뒤 1~2개를 백그라운드로 받는다
- 최근 본 종목 **캐시** (LRU, 수십 개면 충분)
- 첫 화면은 일봉만 그리고 주봉·월봉은 탭할 때 파싱

### 병목 3 — 차트 렌더링

여기는 사실 Flutter가 잘하는 영역이다. 그래도 지킬 것:
- **보이는 구간만 그린다**(viewport clipping). 392봉을 다 그리지 말고 화면에
  들어오는 60~80봉만
- `RepaintBoundary`로 차트와 나머지 UI의 리페인트를 분리
- 패턴선·마커는 캔들과 **다른 레이어**로 두어 확대·이동 시 캔들만 다시 그린다
- 제스처 중에는 지표선 개수를 줄이는 것도 방법 (손을 떼면 복원)

### 측정부터

위는 전부 가설이다. **출시 전에 실제 기기에서 재고 시작한다.**
- 앱 시작 → 첫 검색 결과까지 걸리는 시간
- 스와이프 한 칸 넘기는 데 걸리는 시간
- 차트 확대·이동 시 프레임 드랍 (Flutter DevTools)

느린 곳을 재서 고치지, 느릴 것 같은 곳을 미리 고치지 않는다.

## 단계

- [x] **0. 기술 선택 — Flutter** (2026-08-22 결정)

      운영자가 Flutter·Xcode·안드로이드 스튜디오 경험이 있고 React Native는
      처음이다. 처음 쓰는 틀로 첫 앱을 내는 비용이 크다 — 빌드 설정·스토어
      서명·네이티브 모듈에서 막히는데, 그건 이미 아는 문제들이다.

      React Native를 제안했던 근거("웹 TypeScript 재사용")는 약했다. 실제로
      옮길 건 flags.ts·types.ts·sectors.ts 합쳐 300줄 남짓의 '표'뿐이고,
      무거운 계산은 파이썬 배치가 이미 끝내 JSON으로 내놓는다 — 어떤 언어로
      만들든 그 JSON을 읽으면 된다. 재사용 이점이 거의 없다.

      애드몹도 Flutter 쪽이 낫다: `google_mobile_ads`는 구글이 직접 만든
      공식 플러그인이고 배너·전면·보상형·네이티브를 전부 지원한다
      (RN은 서드파티). 수익화 문서의 광고 조합이 그대로 된다.
- [ ] **1. 뼈대** — 스크리너 목록 / 종목 상세 / 업종맵 3화면. 데이터는
      기존 JSON 그대로.
- [ ] **2. 네이티브 기능** — 관심종목(로컬 저장), 조건 발생 푸시 알림.
      푸시는 서버가 필요하다 — 배치가 만든 latest.json을 읽어 발송하는
      경로를 어디에 둘지 정해야 한다(Vercel 서버리스 or FCM 직접).
- [ ] **3. AdMob** — 기존 계정에 앱 등록, 광고 단위 발급.
      ⚠️ 계정 정리(TODO B-4)와 순서가 얽힌다. 회사/개인 계정을 가르는 중이라
      어느 계정에 앱을 붙일지 먼저 정해야 한다.
- [ ] **4. 스토어 등록** — 개발자 계정, 심사 대응, 면책 고지.

## 하지 않을 것

- 웹 종료. 검색 유입이 웹에서 나온다.
- 배치·데이터 파이프라인 변경. 앱은 소비자일 뿐이다.
- 유료 종목 추천. 유사투자자문업 선은 앱에서도 같다.
